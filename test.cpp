#include <iostream>
#include <cstdlib>
#include <string>


int main(){

	int i =0;
	int x = 1;
	int y = 2;
	std::cout << "Finally got Git to work!" << std::endl;
	/* removed all the other files from staging and only will make chnges to test.cpp.
	 * This is because I ran `git reset` and `git add test.cpp`, therefore only commiting 
	 * test.cpp when I run `git commit`. 
	 */

	std::cout << "Works\n";
}
